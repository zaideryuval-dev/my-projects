#include <stdio.h>
int main() {
	int num, remainder;
	printf("Enter a number:\n");
	scanf_s("%d", &num);
	if (num == 0) {
		printf("the number is: 0");
		return 0;
	}
	while (num > 0) {
		remainder = num % 2;
		printf("%d", remainder);
		num = num / 2;
	}
	return 0;
}
