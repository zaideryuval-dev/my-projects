#include <stdio.h>


int main() {
	int a, b, c, max;
	printf("Enter 3 integer numbers: \n");
	scanf_s(" %d %d %d", &a, &b, &c);
	int the_big_one = (a > b) ? a : b;
	max = (the_big_one > c) ? the_big_one : c;
	printf("the biggest number is: %d\n", max);

	return 0;
}	
